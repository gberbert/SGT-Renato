# Memória Persistente do Agente (RAG)

## Histórico de Decisões e Estado Atual
- **Estado Atual (PRD vs Staging):** Staging (Modo Autônomo).
- **Nota de Contexto:** O projeto atual no diretório é construído com React, Vite e Firebase, diferente da role base de "iOS/watchOS Architect". A stack web está sendo adotada nas decisões.

## Registro de Alterações

- **Data/Hora:** 2026-07-06T22:56:00-03:00
- **O que foi feito:** Criação inicial dos arquivos `.stack_tech.md`, `versionamento.md` e `.agent_memory_rag.md`.
- **Motivo (O Porquê):** Atendimento imediato à atualização de requisitos do arquivo `inicialization.md` que exige a existência destes arquivos.
- **Snapshot de Segurança:** N/A (criação de arquivos).

- **Data/Hora:** 2026-07-06T23:00:00-03:00
- **O que foi feito:** Refatoração substancial nos módulos: `Settings.jsx`, `NewTicketModal.jsx`, `KanbanBoard.jsx` e `Roadmap.jsx`. Adicionado Firebase Services em `settingsService.js` para Sistemas e Componentes.
- **Motivo (O Porquê):** Implementação das regras do "Novo Ticket", transição de tickets com novos campos (`system`, `externalTicket`, `component`), Toggle de Kanban List View, Filtros/Grupos do Roadmap e Gestão de Usuários (shortName).
- **Snapshot de Segurança:** As versões originais dos arquivos React substituídos encontram-se preservadas no cache da ferramenta/commit anterior. Estrutura SPA base preservada sem alteração de build tools.

- **Data/Hora:** 2026-07-07T13:35:00-03:00
- **O que foi feito:** 
  1. Aumento de contraste no `Roadmap.css` (`.bar-progress` de `#7c3aed` para `#4c1d95`). 
  2. Ajuste na cor de finais de semana/feriados (`.custom-holiday-marker`) para `--gray-3` (light) e `--gray-4` (dark). 
  3. Correção na lógica do `Roadmap.jsx` para pintar os finais de semana/feriados corretamente usando `gantt.gantt_start` em vez de buscar a sigla de texto "Sáb/Dom" que não renderizava na visão diária.
- **Motivo (O Porquê):** Pedido visual do usuário (via imagem) para realçar as barras de progresso e garantir que os dias não-úteis no gráfico de Gantt tivessem um fundo cinza visível para contraste.
- **Snapshot de Segurança:** (Roadmap.jsx): `const isWeekend = textContent.includes('Sáb') || textContent.includes('Dom')` e (Roadmap.css): `.custom-holiday-marker { fill: rgba(0, 0, 0, 0.04) !important; }`

- **Data/Hora:** 2026-07-08T13:45:00-03:00
- **O que foi feito:** 
  1. Criação de associação de Tickets e Kanban Board pelo `squadId`.
  2. Implementação do campo e tabela de `roles` para membros de Squad no `SquadDetailsModal`.
  3. Relacionamento de Squad -> System adicionado.
  4. Edição de dados extras da squad (Nome, Descrição).
  5. Adição de tags de squad nos cartões de KanbanBoard.
- **Motivo (O Porquê):** Pedido do usuário para expandir as capacidades de Squad, permitindo melhor governança sobre equipes, vínculos de sistema e papéis específicos exercidos no contexto do projeto.
- **Snapshot de Segurança:** Evolução transparente do modelo de dados de `squad.users` em base legada (array de strings virou array de objetos `{id, role}`) através de parses retrocompatíveis via hook local no `SquadDetailsModal`.
