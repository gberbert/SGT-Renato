# Memória Persistente do Agente (RAG)

## Histórico de Decisões e Estado Atual
- **Estado Atual (PRD vs Staging):** Staging (Modo Autônomo).
- **Nota de Contexto:** O projeto atual no diretório é construído com React, Vite e Firebase, diferente da role base de "iOS/watchOS Architect". A stack web está sendo adotada nas decisões.

## Registro de Alterações

- **Data/Hora:** 2026-07-06T22:56:00-03:00
- **O que foi feito:** Criação inicial dos arquivos `.stack_tech.md`, `versionamento.md` e `.agent_memory_rag.md`.
- **Motivo (O Porquê):** Atendimento imediato à atualização de requisitos do arquivo `inicialization.md` que exige a existência destes arquivos.
- **Snapshot de Segurança:** N/A (criação de arquivos).

- **Data/Hora:** 2026-07-06T23:00:00-03:00
- **O que foi feito:** Refatoração substancial nos módulos: `Settings.jsx`, `NewTicketModal.jsx`, `KanbanBoard.jsx` e `Roadmap.jsx`. Adicionado Firebase Services em `settingsService.js` para Sistemas e Componentes.
- **Motivo (O Porquê):** Implementação das regras do "Novo Ticket", transição de tickets com novos campos (`system`, `externalTicket`, `component`), Toggle de Kanban List View, Filtros/Grupos do Roadmap e Gestão de Usuários (shortName).
- **Snapshot de Segurança:** As versões originais dos arquivos React substituídos encontram-se preservadas no cache da ferramenta/commit anterior. Estrutura SPA base preservada sem alteração de build tools.

- **Data/Hora:** 2026-07-07T13:35:00-03:00
- **O que foi feito:** 
  1. Aumento de contraste no `Roadmap.css` (`.bar-progress` de `#7c3aed` para `#4c1d95`). 
  2. Ajuste na cor de finais de semana/feriados (`.custom-holiday-marker`) para `--gray-3` (light) e `--gray-4` (dark). 
  3. Correção na lógica do `Roadmap.jsx` para pintar os finais de semana/feriados corretamente usando `gantt.gantt_start` em vez de buscar a sigla de texto "Sáb/Dom" que não renderizava na visão diária.
- **Motivo (O Porquê):** Pedido visual do usuário (via imagem) para realçar as barras de progresso e garantir que os dias não-úteis no gráfico de Gantt tivessem um fundo cinza visível para contraste.
- **Snapshot de Segurança:** (Roadmap.jsx): `const isWeekend = textContent.includes('Sáb') || textContent.includes('Dom')` e (Roadmap.css): `.custom-holiday-marker { fill: rgba(0, 0, 0, 0.04) !important; }`

- **Data/Hora:** 2026-07-08T13:45:00-03:00
- **O que foi feito:** 
  1. Criação de associação de Tickets e Kanban Board pelo `squadId`.
  2. Implementação do campo e tabela de `roles` para membros de Squad no `SquadDetailsModal`.
  3. Relacionamento de Squad -> System adicionado.
  4. Edição de dados extras da squad (Nome, Descrição).
  5. Adição de tags de squad nos cartões de KanbanBoard.
- **Motivo (O Porquê):** Pedido do usuário para expandir as capacidades de Squad, permitindo melhor governança sobre equipes, vínculos de sistema e papéis específicos exercidos no contexto do projeto.
- **Snapshot de Segurança:** Evolução transparente do modelo de dados de `squad.users` em base legada (array de strings virou array de objetos `{id, role}`) através de parses retrocompatíveis via hook local no `SquadDetailsModal`.

- **Data/Hora:** 2026-07-08T16:55:00-03:00
- **O que foi feito:** 
  1. Refatoração completa do layout do `TicketDetailsModal.jsx` focando na responsividade e estrutura em tela cheia (mobile) e limites absolutos (desktop).
  2. Implementação do chat de demandas invertido (mais antigas no topo, novas embaixo) usando um `ScrollArea` com alinhamento na base estilo WhatsApp.
  3. Redesenho do cabeçalho do Modal no padrão Jira/Linear (ID + badge no topo, título em destaque embaixo).
  4. Melhoria no sistema de menções do Tiptap: remoção do prefixo `@` e estilização azul via CSS direto no balão de chat.
  5. Criação do superusuário `@Todos` que, ao ser mencionado, aciona o envio em lote de notificações via Firebase para toda a lista de usuários extraída por `getDocs`.
- **Motivo (O Porquê):** Pedidos explícitos do usuário para tornar a área de chat o centro interativo do modal de demandas, com usabilidade semelhante ao WhatsApp e recursos avançados de comunicação e notificação em grupo. A refatoração do Radix UI (flex-grow bug) exigiu cálculos de altura absolutos (`calc`) para evitar o colapso dos componentes.
- **Snapshot de Segurança:** Remoção da dependência exclusiva do `flex-grow` nas abas do Radix UI em favor de alturas explícitas, garantindo consistência trans-navegador no colapso de ScrollAreas.

- **Data/Hora:** 2026-07-08T17:00:00-03:00
- **O que foi feito:** 
  1. Remoção do fluxo de "Criar Conta" (`createUserWithEmailAndPassword`) da tela inicial `Login.jsx`.
  2. Implementação do fluxo de "Esqueci minha senha" (`sendPasswordResetEmail`) acessível pelo mesmo botão/link inferior.
- **Motivo (O Porquê):** Pedido do usuário para fechar o sistema apenas para convidados/cadastros internos. Novos usuários agora só poderão ser criados por Administradores na tela de Gestão de Usuários.
- **Snapshot de Segurança:** Remoção de `createUserWithEmailAndPassword` no componente Login.

- **Data/Hora:** 2026-07-09T13:30:00-03:00
- **O que foi feito:** 
  1. Novo motor de criação e autogeração de Atividades a partir de Estimativas de Esforço (`EstimationEditorModal.jsx`).
  2. Implementação de "Cascade Updates": ao atualizar a Squad de uma Demanda Pai, a mudança é injetada instantaneamente no Firestore para todas as Atividades filhas.
  3. Separação visual: Demandas ficam no Kanban de Demandas, Atividades vão para o Kanban de Atividades.
  4. Componente de UI: Exibição inteligente de badge (ex: DEM-777) substituindo o ticket ID vazio de filhas na UI, com fallbacks corrigidos em caixas Radix UI `Select` antes defeituosas para strings vazias.
  5. Remoção do Forçar Admin de UI e busca dinâmica do papel via tabela `users`.
- **Motivo (O Porquê):** Pedidos do usuário para refinar o fluxo de "Demanda -> Estimativa -> Atividade", além de corrigir falhas visuais herdadas.
- **Snapshot de Segurança:** Adição do fallback `value="none"` nos Selects de `TicketDetailsModal.jsx` para suportar ausência de squad/responsável sem colapsar UI Radix.

- **Data/Hora:** 2026-07-12T09:50:00-03:00
- **O que foi feito:** Atualização do diagrama UML na tela de "Ajuda & Processos" (`HelpFlow.jsx`) para a etapa de "Criação da Demanda", adicionando passos automatizados do sistema.
- **Motivo (O Porquê):** Inclusão da representação visual de que o sistema cria automaticamente registros de T-Shirt, Estimativas, Espc. Func. e Espc. Técnica (estes últimos 1 para cada sistema) com status Pendente.
- **Snapshot de Segurança:** (HelpFlow.jsx): UML sequenceDiagram antigo possuía apenas o fluxo até `Demanda->>Kanban: Cria Card na primeira etapa do Kanban`.

- **Data/Hora:** 2026-07-12T09:51:30-03:00
- **O que foi feito:** Atualização do diagrama UML na tela de "Ajuda & Processos" (`HelpFlow.jsx`) para a etapa de "Análise & Alocação".
- **Motivo (O Porquê):** Ajuste para refletir que o Team Leader aloca as Atividades geradas (T-Shirt, Estimativa, EF, ET, Dev) ao Desenvolvedor e não a Demanda completa (já que ela foi dividida nas etapas anteriores).
- **Snapshot de Segurança:** (HelpFlow.jsx): Diagrama original referenciando "Ticket as Demanda" mantido via Git e RAG anterior.

- **Data/Hora:** 2026-07-12T09:54:30-03:00
- **O que foi feito:** Separação do bloco "Geração de T-Shirt e Estimativas" em duas etapas distintas no componente `HelpFlow.jsx`.
- **Motivo (O Porquê):** Pedido do usuário para separar T-Shirt (que segue fluxo simples) de Estimativas, que agora indica de forma explícita no diagrama a criação de um card de Desenvolvimento no Kanban para cada funcionalidade listada no sistema da estimativa.
- **Snapshot de Segurança:** (HelpFlow.jsx): Bloco `entregaveis_tshirt_est` antigo mantido no commit anterior antes do split.
