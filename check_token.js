const admin = require('firebase-admin');
const serviceAccount = require('./firebase.json'); // wait, we don't have service account key
// we can use firebase functions shell or firebase firestore:get
